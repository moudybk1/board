# Theme backups

## `backup-phosphor-crt`

Previous BOARD look: charcoal CRT cabinet + mint phosphor accent (dark forest felt).

### Restore

```bash
cp src/app/themes/backup-phosphor-crt/globals.css src/app/globals.css
```

Then hard-refresh the app.
